# ============================================================
# requirements.txt — Projet 05 : Agrégateur météo multi-sources
# ============================================================
# Toutes les dépendances du projet avec leur rôle documenté.
# Installer avec : pip install -r requirements.txt
# ============================================================

# --- Framework web ---
fastapi==0.111.0          # Framework principal (ch.05 et ch.06 du cours)
uvicorn[standard]==0.29.0 # Serveur ASGI pour lancer FastAPI

# --- Validation des données ---
pydantic[email]==2.7.1    # Schémas de validation (ch.06 du cours)
pydantic-settings==2.3.0  # Chargement et validation des variables d'environnement

# --- Appels HTTP asynchrones ---
httpx==0.27.0             # Client HTTP async (ch.06 exemple_01_async_route)

# --- Cache Redis ---
redis==5.0.4              # Client Python pour Redis (cache 2 niveaux)

# --- Planificateur de tâches ---
apscheduler==3.10.4       # Tâche de pré-chauffe toutes les 10 min

# --- Logging structuré ---
python-json-logger==2.0.7 # Formateur JSON pour les logs (contrainte transverse du sujet)

# --- Monitoring Prometheus ---
prometheus-fastapi-instrumentator==6.1.0  # Métriques HTTP automatiques + endpoint /metrics
prometheus-client==0.20.0                 # Compteurs, histogrammes et jauges custom

# --- Tests ---
pytest==8.2.0             # Framework de tests (ch.08 du cours)
pytest-asyncio==0.23.6    # Support des tests async avec pytest
pytest-cov==5.0.0         # Mesure de couverture de code (objectif : 70%)
respx==0.21.1             # Mock des appels httpx en tests (ch.08 + difficulté)
fakeredis==2.26.1         # Redis en mémoire pour les tests unitaires du cache
httpx==0.27.0             # Aussi utilisé par le TestClient FastAPI