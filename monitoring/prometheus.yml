global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    project: "agregateur-meteo"

scrape_configs:
  - job_name: "meteo-agregateur"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["app:8000"]
    scrape_interval: 10s
