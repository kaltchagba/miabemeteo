apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus-meteo
    url: http://prometheus:9090
    access: proxy
    isDefault: true
    editable: false
    jsonData:
      timeInterval: "10s"
      httpMethod: POST
